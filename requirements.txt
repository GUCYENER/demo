 # --- FastAPI & Server ---
fastapi
uvicorn[standard]

# --- Environment ---
python-dotenv

# --- Data / DB ---
pydantic
pydantic-settings
sqlalchemy
alembic
psycopg2-binary

# --- Oracle DB driver (v3.26.0 Faz 1 P0-b) ---
# Mevcut kod (ds_learning_service.py:72) `oracledb` import ediyor — requirements'a eklendi.
# Thick mode (Instant Client gerekli) opsiyonel; thin mode default.
oracledb>=2.0.0

# --- Observability (opsiyonel — v3.26.0 Faz 5 P2-b) ---
# Langfuse 3rd-party observability. LANGFUSE_PUBLIC_KEY/SECRET_KEY ile aktif
# olur; aksi halde langfuse_adapter NO-OP'tur. DB-tabanlı pipeline_events
# her hâlükârda yazılır.
langfuse>=2.0.0; python_version >= "3.10"

# --- Password & Auth ---
bcrypt>=4.0.0
passlib[bcrypt]
python-jose[cryptography]
python-multipart
ldap3

# --- LLM / Embeddings ---
google-generativeai
sentence-transformers
numpy

# --- PDF / Document Processing ---
pypdf
python-docx
openpyxl
xlrd
python-pptx
fpdf2>=2.7.0

# --- OCR (Taranmış PDF Desteği) ---
easyocr
pdf2image

# --- PDF Processing (PyMuPDF) ---
PyMuPDF>=1.24.0

# --- ONNX Runtime (Embedding Optimizasyonu) ---
onnxruntime>=1.17.0

# --- Redis Cache ---
redis>=5.0.0

# --- XLSX Output ---
xlsxwriter>=3.1.0

# --- Networking ---
requests

# --- Machine Learning (CatBoost Hybrid) ---
catboost>=1.2.0
rapidfuzz>=3.0.0

# --- Utils ---
tqdm

# --- Security / Rate Limiting ---
slowapi>=0.1.9

# --- LDAP / Active Directory ---
ldap3>=2.9.0
cryptography>=41.0.0

# --- Testing ---
pytest>=9.0.0
pytest-cov>=7.0.0
alembic
bcrypt
cryptography
fastapi
fpdf2
ldap3
numpy
openpyxl
passlib[bcrypt]
psycopg2-binary
pydantic
pydantic-settings
pypdf
python-docx
python-dotenv
python-jose[cryptography]
python-multipart
python-pptx
rapidfuzz
requests
slowapi
sqlalchemy
tqdm
uvicorn[standard]
xlrd
