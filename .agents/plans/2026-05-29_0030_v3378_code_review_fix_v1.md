---
plan_id: v3378_code_review_fix
created: 2026-05-29
branch: hira
status: completed
version_target: v3.37.8
council_mod: 2
hebe_gate_required: true
---

# v3.37.8 — Code-Review (wf_1da517ba) 15 Finding Consolidated Fix

## Context

v3.37.7 commit'i sonrası `/code-review xhigh` skill (workflow `wrctmcj31`, 9 angle × 8 candidate × verify × sweep) **15 finding** ortaya çıkardı (52 PLAUSIBLE/CONFIRMED survivors). v3.37.7 fix paketi yarı kalmıştı + iki yeni regresyon/güvenlik açığı yarattı:

- **F1/F11:** Case asymmetry (write `.lower()`, read raw) — `host='Host'` legacy row silent failure
- **F2:** Canary set diverged (`db_password` write'da var, read'de yok) — SSOT iddia yalan
- **F3:** Sadece `host` validate; `db_name`/`db_user`/`db_password` placeholder geçebilir
- **F4:** Whitespace → None coercion; source_type='database' için NULL host kabul ediliyor
- **F5/F9:** FE wizard_state fallback kaldırıldı, BE hâlâ izinli → pre-v3.30 legacy reports broken
- **F6:** Error fallback `slice(0,200)` admin_detail JSON leak (yeni error_code'lar için)
- **F7:** 400 detail `field`+`source_id`+`admin_detail` non-admin'e wire'a basılıyor
- **F8:** Status 500→400 semantik regresyon (ops 5xx-rate alerting kaybı, retry middleware durur)
- **F10:** `buildFriendlyMessage` dict detail için kırık (`'[object Object]'`)
- **F14:** BE wizard_state fallback dead code + yorum yalan
- **F12:** SharePoint tenant validator mesajı semantik yanlış

## Mevcut Durum

- Code-review workflow output: `/tmp/claude-1000/-mnt-d-demo-vyra/.../tasks/wrctmcj31.output`
- Council görüş: HERMES + ATHENA + ARES + HEBE + APOLLO 7/7 ✅
- Kullanıcı onayı: "onayla devam et" 2026-05-29 ~00:00

## Faz/Gate Haritası

| Gate | Sorumlu | Bulgu kapsama |
|---|---|---|
| G1 | HERMES + ARES | `dialect_constants.py` SSOT canary (`HOST_VALUE_CANARIES` + `is_canary_value`) — F1/F2/F11 |
| G2 | HERMES | `data_sources_api.py` SSOT'a göç + `@field_validator` `host`/`db_name`/`db_user`/`db_password` + `@model_validator` cross-field (source_type ∈ database/ftp/sharepoint/file_server → host required) — F3/F4 |
| G3 | HERMES + ARES | `db_smart_api.py` `_load_source` SSOT'a göç + `is_canary_value` (case-insens) + status 400→500 geri + `admin_detail` wire'dan kaldır (log-only) — F1/F7/F8/F11 |
| G4 | ATHENA + ARES | `report_detail_modal.js` BE wizard_state fallback'i aktive et (body'ye ekle); effectiveSourceId = source_id ‖ ws.source_id (sadece BE fallback için), legacy reports rerun çalışır — F5/F9/F14 |
| G5 | ATHENA + ARES | `report_detail_modal.js` error fallback `slice(0,200)` raw leak → generic "Sunucu hatası (HTTP X)" — F6 |
| G6 | ATHENA | `api_client.js` `buildFriendlyMessage` dict-detail handle (`_extractDetailMessage`) — F10 |
| G7 | ATHENA + HEBE | `data_sources_module.js` SharePoint validator mesajı düzelt + `db_name`/`db_user`/`dbPassword` canary kontrolleri — F3/F12 |
| G8 | TYCHE | 20 senaryo Pydantic + SSOT canary + cross-field validation — Windows venv |
| G9 | HERA | README v3.37.8 entry + versiyon + bundle rebuild + commit |
| G10 | code-review skill | `/code-review medium` v3.37.8 commit'i için (ikinci geçiş — yeni regresyon yok mu doğrula) |

## Critical Files

- **EDIT** `app/services/db_smart/dialect_constants.py` — `HOST_VALUE_CANARIES` + `is_canary_value`
- **EDIT** `app/api/routes/data_sources_api.py` — SSOT import, 4 field_validator, cross-field model_validator
- **EDIT** `app/api/routes/db_smart_api.py` — SSOT import, _load_source canary case-insens, 500 geri, admin_detail log-only, yorum düzeltme
- **EDIT** `frontend/assets/js/modules/report_detail_modal.js` — wizardState body, effectiveSourceId, error fallback hardening
- **EDIT** `frontend/assets/js/api_client.js` — `_extractDetailMessage` + buildFriendlyMessage dict-detail
- **EDIT** `frontend/assets/js/modules/data_sources_module.js` — SharePoint mesaj + ek canary kontrolleri
- **EDIT** `README.md` — v3.37.8 entry
- **REBUILD** `frontend/dist/bundle.min.{js,css}`

## Council konsensüs

- HERMES ✅ "SSOT canary tek liste; case-insens her iki tarafta; model_validator cross-field net"
- ATHENA ✅ "FE wizard_state body'ye ekleme legacy compat; error fallback generic mesaj"
- ARES ✅ "admin_detail log-only; 500 status geri çevirme defansif observability"
- HEBE ✅ "SharePoint mesaj UX semantik düzeltildi"
- APOLLO ✅ "Kök neden + knock-on aynı sprintte"
- HEPHAESTUS ✅ "Migration opsiyonel; SSOT canary table-level CHECK constraint için altyapı"
- TYCHE ✅ "20/20 PASS — case-insens, cross-field, manual_file edge"

**Kullanıcı onayı:** ✅ ("onayla devam et" 2026-05-29 ~00:00)

## Out-of-scope (v3.37.9+)

- DB CHECK constraint (`host NOT IN ('host','port',...)` via migration) — opsiyonel altyapı
- `_load_source` aynı kontrolleri db_user/db_name için (bu sprintte yalnız host)
- saved_reports.source_id NOT NULL constraint (migration 048+) — backfill audit ile birlikte
