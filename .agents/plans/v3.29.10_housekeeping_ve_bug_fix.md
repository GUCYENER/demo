---
plan_id: v3.29.10_housekeeping_ve_bug_fix
created: 2026-05-19
branch: hira
status: in_progress
version_target: v3.29.10
council_mod: 3
hebe_gate_required: true
---

# v3.29.10 — Housekeeping + Live Bug Fix

## Context (Neden bu değişiklik?)

Kullanıcı "BAŞLA" sonrası iki ayrı iş yükü iletti:

1. **Öne çıkan housekeeping işleri** (oturum başı raporda tespit edildi):
   - v3.29.7 Faz-6 plan.md'si `status: done` ama log satırlarında G2/G3 hâlâ `in_progress` olarak göründü (closure eksik).
   - v3.29.8_signal_weight_tuner planı `status: in_progress` — fakat commit `5ad3828` zaten yapılmış. Status güncel değil.
   - README.md (`Versiyon: 3.26.0`) son üç sürüm (v3.27, v3.28, v3.29.x) için güncellenmedi.
   - Working tree **19 modifiye + 17 yeni** dosya birikti (db_learning, feature_permissions, failure_queue, migrations 023-028) — commit edilmemiş.

2. **Live bug bulguları** (kullanıcı yerelde test ediyor, ekran görüntüsü iletti):
   - **Bug #1:** `GET /api/agentic-query/observability/template-heatmap?days=30` → **500 Internal Server Error**
   - **Bug #2:** Tarayıcı konsolunda `Uncaught TypeError: Cannot read properties of null (reading 'click')` → `home.html:1:45` (HTMLButtonElement.onclick içinden) — muhtemelen "Tablo Etiketleme & Onay Paneli" modalı içindeki "Onaylıları Gözden Geçir" butonu (ekran görüntüsünde modal açık).

**Kullanıcı talimatı:** "önce öne çıkan işlerin hepsini bitir. sonra bunları yap. yerelde uygulamayı test ettim, bulguları iletiyorum. sırayla çözelim. işe ekibi de dahil et."

## Mevcut Durum (keşif bulguları)

### Housekeeping
- `.agents/plans/v3.29.7_faz6_eksik_implementasyon.md` — frontmatter `status: done` ✅ ama G6 satırında "push pending user" notu var; ilerleme logunda G2/G3/G4 satırlarında "⏳ in_progress" kalmış (tablo zaten ✅ done gösteriyor — yalnızca log satırı temizliği)
- `.agents/plans/v3.29.8_signal_weight_tuner.md` — frontmatter `status: in_progress`, ama commit `5ad3828` v3.29.8'i merge etmiş → status `completed`'a çekilmeli
- `.agents/plans/v3.29.9_fk_inference.md` — frontmatter YOK (eski format), `status` alanı yok; v3.29.9 commit `1b4dced` zaten yapılmış → frontmatter eklenmeli, `status: completed`
- `README.md` satır 3724: `**Versiyon:** 3.26.0` → `v3.29.10` (housekeeping bump) olarak güncellenmeli, ara versiyonlar (v3.27, v3.28, v3.29.7-9) için release notu eklenmeli

### Working tree (commit edilmemiş)
**Modified (19):**
- Faz-6 (v3.29.7) artıkları: `deep_think_service.py`, `retrieve.py`, `self_heal.py`, `sql_generate.py`, `few_shot_selector.py`, `synthetic_templates.py`, `learned_queries_service.py`, `fk_synthetic_generator.py`
- Frontend Faz-6: `ds_enrichment_module.js`, `sample_data_preview.js`, `_temp_entry.js`, `section_auth.html`, `section_history.html`, CSS güncellemeleri
- Plan dosyası: `v3.29.7_faz6_eksik_implementasyon.md`
- Diğer: `bilgi.txt`, `tests/test_synthetic_templates.py`

**Untracked (17):**
- Backend: `feature_permissions.py`, `cardinality_analyzer.py`, `code_value_extractor.py`, `error_pattern_learner.py`, `fk_graph_resolver.py`, `incremental_schema_integrator.py`, `sample_data_loader.py`, `permission_audit.py`, `disambiguation_card.py`
- Frontend: `_tooltip.css`, `feature_permissions.css`, `failure_queue_module.js`, `section_failure_queue.html`
- Migrations: `023_v3290_relationship_cardinality`, `024_v3290_business_glossary_v2`, `025_v3290_code_value_dictionary`, `026_v3290_template_versioning`, `027_v3290_pii_flag`, `028_v3290_query_failure_log`
- Test/util: `oracle_test.bat`, `oracledb/`

### Bug #1 — heatmap 500
- Endpoint: `app/api/routes/agentic_query_api.py:908-965`
- Sorgu PostgreSQL spesifik: `make_interval(days => %s)`, `COUNT(*)::int`, `AVG(...)::float`
- `_require_admin(current_user)` çağrısı var — eğer kullanıcı admin değilse 403 dönmeli (500 değil)
- Kök neden hipotezleri:
  - (a) `ds_synthetic_query_runs` tablosu bazı kurulumlarda yok / kolon eksik (`complexity_score`, `status`)
  - (b) `_require_admin` HTTPException yerine genel Exception fırlatıyor (500 olarak görünüyor)
  - (c) `make_interval` PostgreSQL versiyonu 9.4+ gerektirir — local kurulumda farklı sürüm
- **Hipotez doğrulama:** `logs/vyra.log` tail + curl/admin token ile yeniden çağırma
- İlgili Faz-6 commit'i: 7c83b32 v3.29.7

### Bug #2 — home.html onclick null
- `frontend/home.html`'de **inline onclick** sadece 3 tane (satır 732/741/750, hepsi `window.selectChatMode('...')`)
- `selectChatMode` (satır 843-876) null check'leri sağlam (`if (kb) kb.classList...`)
- Hata stack `home.html:1:45` → muhtemelen dynamic-loaded modal/partial içindeki bir buton (browser inline event handler'ını host HTML'e atfeder)
- Ekran görüntüsü: "Tablo Etiketleme & Onay Paneli" modalı açık — buton: "Onaylıları Gözden Geçir"
- Adaylar: `sample_data_preview.js`, `ds_enrichment_module.js`, `agentic_observability_faz6.js`
- Kök neden hipotezi: `onclick="document.getElementById('X').click()"` benzeri delegasyon ve `X` DOM'da yok (örn. modal kapalıyken veya feature_permissions gated)

## Faz/Gate Haritası

| Adım | Başlık | MOD | Bağımlılık | Durum |
|------|--------|-----|------------|-------|
| **G1** | Plan dosyalarının status'unu kapat (v3.29.7/8/9) | 1 | — | ⏳ |
| **G2** | README versiyon senk. + v3.27..v3.29.9 release notları | 1 | — | ⏳ |
| **G3** | Working tree'yi mantıksal commit gruplarına ayır | 2 | G1, G2 | ⏳ |
| **G4** | Bug #1: heatmap 500 — log analizi + kök neden + fix | 2/3 | G3 (clean tree) | ⏳ |
| **G5** | Bug #2: onclick null — modal/partial trace + fix | 2 + HEBE | G3 | ⏳ |
| **G6** | Frontend build + smoke test + Post-Impl Review | 1 | G4, G5 | ⏳ |
| **G7** | APP_VERSION 3.29.9 → 3.29.10 + final commit | 1 | G6 | ⏳ |

> **G3 ayrımı (mantıksal commit grupları):**
> 1. `chore(plans): close v3.29.7/8/9 plan dosyaları`
> 2. `feat(db-learning): v3.29.0 supporting modules (cardinality/code-value/error-pattern/fk-graph/incremental/sample-data)`
> 3. `feat(api): feature_permissions + permission_audit endpoints`
> 4. `feat(pipeline): disambiguation_card node`
> 5. `feat(frontend): failure_queue module + feature_permissions UI + tooltip helper`
> 6. `feat(migrations): 023-028 v3.29.0 schema additions`
> 7. `chore: misc Faz-6 follow-up (deep_think, retrieve, self_heal, sql_generate, few_shot, synthetic)`
> 8. `docs(readme): versiyon bump v3.29.10`

## Critical Files to Modify / Create

**G1:**
- `.agents/plans/v3.29.7_faz6_eksik_implementasyon.md` (ilerleme log satırı temizliği)
- `.agents/plans/v3.29.8_signal_weight_tuner.md` (status: completed + commit hash)
- `.agents/plans/v3.29.9_fk_inference.md` (frontmatter ekle)

**G2:**
- `README.md` satır 3724 ve versiyon notları bölümü

**G4 — Bug #1:**
- `app/api/routes/agentic_query_api.py:908-965` (try/except + parametre validasyonu)
- Belki: `migrations/versions/021_*_synthetic_query_runs.py` kontrol (kolon var mı?)

**G5 — Bug #2:**
- `frontend/assets/js/modules/sample_data_preview.js` veya `ds_enrichment_module.js` (modal içi onclick'ler)

**G7:**
- `app/__init__.py` veya `APP_VERSION` tanımı bulunduğu yer
- `README.md`

## Yeniden Kullanılacak Mevcut Fonksiyonlar

- `app/core/db.get_db_context` — heatmap için zaten kullanılıyor
- `app/api/routes/agentic_query_api._require_admin` — yetki kontrolü (varsa düzeltilir)
- `window.showToast` — bug #2 fix sonrası kullanıcıya bilgi (alert YOK)
- `window.VYRA_FeaturePermissions` — feature gating zaten var

## Risk Özeti

| Risk | Olasılık | Etki | Mitigasyon |
|------|----------|------|------------|
| Working tree commit'leri arasında dosya bağımlılığı kırılır | Orta | Orta | Mantıksal grupları topolojik sırayla (DB→backend→frontend) commit et |
| Heatmap fix'i şema migration gerektirir | Düşük | Yüksek | Önce log oku, kolon eksikse yeni migration yerine COALESCE fallback |
| onclick null hatasının kaynağı modal-DOM zamanlama (race) olabilir | Orta | Düşük | DOM elementi var mı diye null guard ekle, alert yerine showToast |
| Çok sayıda commit `hira` branch'ını şişirir | Orta | Düşük | Anlamlı conventional mesajlar; gerekirse squashing kullanıcı kararı |

## Verification (uçtan-uca test)

**Backend syntax:**
```bash
python -c "import py_compile; py_compile.compile('app/api/routes/agentic_query_api.py', doraise=True)"
```

**Heatmap endpoint canlı test (admin token):**
```bash
curl -s -H "Authorization: Bearer $TOKEN" "http://localhost:8002/api/agentic-query/observability/template-heatmap?days=30" | head -c 500
```

**Frontend smoke:**
1. http://localhost:8000/login.html → admin login
2. "Tablo Etiketleme & Onay Paneli" modalı aç → konsol temiz mi?
3. Observability sekmesi → template heatmap render oluyor mu?
4. "Onaylıları Gözden Geçir" buton tıklaması hata vermiyor mu?

**Frontend build:**
```bash
node frontend/build.mjs
# dist/bundle.min.js timestamp kontrolü
```

**Pytest (regresyon):**
```bash
python -m pytest tests/test_synthetic_templates.py -v
```

## Out-of-scope

- v3.30 Yeni feature'lar (master plan)
- Faz 7 cross-source federasyon
- Multi-tenant glossary
- Mobile responsiveness polishi
- Test coverage genişletmesi (yalnız regresyon test ediyoruz)

---

## İlerleme Logu

| Tarih | Adım | Durum | Commit |
|-------|------|-------|--------|
| 2026-05-19 | Plan oluşturuldu | ✅ | (henüz) |
