---
plan_id: r022_stale_tests
created: 2026-06-02
branch: hira
status: completed
version_target: v3.51.0
closed: 2026-06-02
closure_note: "72/72 passed (8 fail→0). 1 kod fix (explain dual-key columns||select) + 6 stale test güncellemesi. Tüm davranışlar changelog'da kasıtlı doğrulandı (regresyon değil). Code-review temiz."
council_mod: 3
hebe_gate_required: false
---

# R022 — test_api_db_smart.py 8 pre-existing failure (stale test + 1 kod bug)

## Context
8 test branch hira HEAD'de fail (a23648e'de de). Tanı (tam traceback): kod kasıtlı evrildi,
testler güncellenmedi. BİR tanesi gerçek kod tutarsızlığı ortaya çıkardı (explain dual-key).

## Tanı (kanıtlı — her biri kod regresyonu DEĞİL, kasıtlı davranış)
| Test | Hata | Kök | Aksiyon |
|---|---|---|---|
| explain_renders_and_caches | KeyError total_cost | post_explain_ast `_select_empty` yalnız `ast.get("select")` bakıyor; kanonik key `columns` (ast_renderer:437) → "columns"-AST has_ast:false → EXPLAIN çalışmaz | **KOD FIX** (dual-key) |
| explain_invalid_ast_400 | 200 (bekl 400) | aynı kök — "columns"-AST render edilmiyor → ValueError→400 hiç olmuyor | **KOD FIX** (dual-key) ile çözülür |
| explain_rejects_non_select | 200 (bekl 400) | F17 (2026-05-25): non-select/eksik AST → 200 has_ast:false (graceful, render/exec YOK) KASITLI | test: assert 200+has_ast False |
| list_columns_envelope | 404 (bekl 200) | v3.38.0 tablo-yetki gate; mock scope kurmuyor → _fetch_table_columns None → 404 (doğru) | test: mock scope+_fetch_table_columns |
| list_saved_reports | TypeError name_exact | v3.41.5 endpoint name_exact kwarg ekledi | test: lambda name_exact=None |
| mark_run_success | TypeError snapshot | v3.38.7 mark_run snapshot kwarg ekledi | test: lambda snapshot=None |
| execute_stream_returns_sse | 403 (bekl 200) | v3.40.0 gate resolve_scope→table_guard.enforce_sql_scope; test resolve_scope mock'luyor (yanlış hedef) | test: enforce_sql_scope mock |
| execute_stream_rejects | 'yetkiniz' wording | v3.39.2 _scope_restricted_message reworded; 403 DOĞRU | test: enforce_sql_scope mock + mesaj assert |

## Kod fix (G1 — HERMES)
`post_explain_ast` `_select_empty`: `ast.get("columns") or ast.get("select")` (columns primary,
select fallback — ast_renderer canonical ile bire bir). DEFENSIVE: FE hangi key'i gönderirse
göndersin doğru (regresyon imkansız). Düzeltme EXPLAIN'i normal AST'ler için RESTORE eder
(eskiden has_ast:false ile sessizce devre dışıydı; 5sn cache perf'i sınırlar — endpoint zaten
EXPLAIN için tasarlanmış, docstring).

## Test fixleri (G2 — TYCHE) — 6 test
Yukarıdaki tabloya göre. explain_renders_and_caches + invalid_ast KOD fix ile düzelir (test edit YOK).

## Critical Files
- `app/api/routes/db_smart_api.py` (post_explain_ast _select_empty)
- `tests/db_smart/test_api_db_smart.py` (6 test)
- `.agents/refactor/REFACTOR_BACKLOG.md` (R022 → done)
- `app/core/config.py` (versiyon v3.51.0)

## Verification
- `pytest tests/db_smart/test_api_db_smart.py -q` → 72 passed (8 fail → 0)
- Kod fix diğer explain testlerini (cache_ttl_expires) kırmamalı (dual-key her iki key'i kabul)
- /code-review medium

## Risk
| Risk | Mit |
|---|---|
| explain fix EXPLAIN'i aktifleştirir (perf) | 5sn cache + endpoint zaten bunun için tasarlı; "columns"-AST'te EXPLAIN doğru davranış |
| test'i körlemesine geçirme (regresyon gizleme) | her davranış changelog'da kasıtlı doğrulandı; ARES explain fix VALIDATION'ı güçlendirir |

## Out-of-scope
- Diğer test dosyalarındaki olası stale testler (bu görev yalnız test_api_db_smart.py 8 failure).
</content>
