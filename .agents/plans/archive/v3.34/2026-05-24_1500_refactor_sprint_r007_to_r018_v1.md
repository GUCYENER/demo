---
plan_id: refactor_sprint_r007_to_r018
created: 2026-05-24
branch: hira
status: completed
version_target: v3.34.1
council_mod: 3
hebe_gate_required: true
---

# Refactor Sprint — R007..R018 Sequential Resolution

## 1. Context

Kullanıcı talebi: `REFACTOR_BACKLOG.md`'da `status: open` olan **tüm** maddeleri sırayla çözmek + code review yapmak. Sprint kapanışında commit kararı kullanıcıya bırakılacak.

Açık maddeler (9 adet — öncelik sırası):

| # | ID   | Pri | Risk     | Effort | Domain       | Notes |
|---|------|-----|----------|--------|--------------|-------|
| 1 | R018 | P0  | critical | S      | Backend/Auth | IDOR — derhal, sprint'i kilitliyor |
| 2 | R007 | P2  | low      | S      | DB           | mig 045 index |
| 3 | R008 | P3  | low      | XS     | DB           | mig 045 rename |
| 4 | R009 | P3  | low      | XS     | DB           | mig 045 CHECK constraint |
| 5 | R010 | P3  | low      | XS     | Docs         | Runbook note only |
| 6 | R013 | P3  | low      | XS     | Frontend CSS | cursor:not-allowed |
| 7 | R014 | P3  | low      | M      | Frontend JS  | poll state-hash guard |
| 8 | R016 | P2  | low      | L      | Frontend API | vyraFetch SSE/blob variants |
| 9 | R017 | P2  | low      | M      | Frontend     | vyraFetchUI wrapper (additive) |

R007/R008/R009 tek migration dosyasında birleştirilir (`045_v3341_audit_index_cap.py`).

## 2. Mevcut Durum (Explore bulguları)

- `app/api/routes/query_builder_api.py:190-205` — `_resolve_dialect_and_source` company_id filtresiz (IDOR primary).
- `app/api/routes/query_builder_api.py:380-384` — `apply_company_scope` bare-except pass (silent RLS bypass — R006 pattern).
- `migrations/versions/044_v3330_rls_canonical_with_check.py` — son migration; yeni dosya 045 numarasıyla.
- `frontend/assets/js/api_client.js` — vyraFetch JSON-only (R016 base).
- `frontend/assets/js/modules/dialog_chat.js:551,1433,2656,2988` — 4 raw fetch (SSE×2 + blob×2).
- `frontend/assets/js/modules/ds_enrichment_module.js:577` — `<input type="checkbox" class="ds-bulk-chk">` disabled cursor yok.
- `frontend/assets/js/modules/ds_enrichment_module.js:201-251` — `_pollRunningJob` her tick'te `applyFilterAndRender()`.

## 3. Faz/Gate Haritası

| Gate | Madde | Strateji | Review gate |
|------|-------|----------|-------------|
| G1 | R018 | `_resolve_dialect_and_source(source_id, company_id)` + tenant WHERE; `apply_company_scope` fail-loud (her iki endpoint); 404 on miss | ARES + HERMES + HEPHAESTUS + TYCHE |
| G2 | R007+R008+R009 | mig 045: CREATE INDEX idx_schema_warn_enrich; ALTER INDEX RENAME; ALTER detail CHECK ≤1000 | HEPHAESTUS + POSEIDON |
| G3 | R010 | `data_sources_api.py:1799` docstring + `migrations/versions/045_*.py` header note | HERA |
| G4 | R013 | `frontend/assets/css/global.css` global `[disabled]` cursor — scoped fallback | HEBE + ATHENA |
| G5 | R014 | `_pollRunningJob` state hash guard; `_lastPollHash` modül kapsamlı | ATHENA + NIKE |
| G6 | R016 | `api_client.js` vyraFetch + `{ responseType: 'stream' \| 'blob' \| 'json' }`; geri uyumlu | HERMES + ARES |
| G7 | R017 | `api_client.js` `vyraFetchUI(path, opts, { onError, onSuccess, loadingEl })` ek wrapper; non-breaking | HERMES + HEBE |
| G8 | Code Review | Diff özet, syntax/import check, regresyon test | HERMES + ARES + TYCHE + HEPHAESTUS + HEBE + ATHENA |

## 4. Critical Files to Modify / Create

**Modify:**
- `app/api/routes/query_builder_api.py` (G1)
- `app/api/routes/data_sources_api.py` (G3 docstring)
- `frontend/assets/css/global.css` (G4)
- `frontend/assets/js/modules/ds_enrichment_module.js` (G5)
- `frontend/assets/js/api_client.js` (G6, G7)

**Create:**
- `migrations/versions/045_v3341_audit_index_cap_rename.py` (G2, G3 header)

**Out of scope this sprint:**
- R016 dialog_chat.js call-site migration → SSE/blob variant kullanılır kullanılmaz **ayrı PR** (call-site refactor). R016 satırı backlog'da `open → done(api-side)` olarak güncellenir, call-site migration için R019 açılır.
- R017 28 modülde call-site refactor → wrapper ile geri uyumlu; opt-in adoption ayrı PR. R017 backlog `open → done(wrapper-added)` olarak güncellenir, modül migration için R020 açılır.

## 5. Yeniden Kullanılacak Mevcut Fonksiyonlar

- `apply_company_scope` (app/api/routes/agentic_query_api.py) — R018'de fail-loud çağrılır.
- `get_db_context` — DB context manager.
- `_IDENT_RE` whitelist — query_builder_api.py içinde mevcut.
- `vyraFetch` mevcut JSON path — R016/R017 additive, mevcut çağrılar bozulmaz.

## 6. Risk Özeti

| Risk | Olasılık | Etki | Mitigasyon |
|------|----------|------|------------|
| R018 fix mevcut tenant happy-path'i bozar | low | high | Same-tenant integration test, log warn-on-miss |
| Migration 045 prod'da long-lock (ALTER) | low | med | `IF NOT EXISTS`, `CONCURRENTLY` mümkün değilse rapid; minimal lock — küçük tablo |
| R014 state-hash kaçırılan transition | low | low | Hash key `(type, started_at, id)` üçlüsü; tick eksiği değil duplicate-skip riski |
| R016 yeni responseType param sözleşme kırar | very low | low | Default `'json'`, mevcut callers değişmez |
| R017 wrapper adoption tutarsız | low | low | Wrapper additive; adoption R020 PR'da plan'lanır |

## 7. Verification (uçtan-uca test)

- **G1 (R018):** Manuel — login as tenant A, POST `/api/query-builder/preview` with `source_id` of tenant B → expect 404. Same-tenant baseline → 200.
- **G2 (R007/R008/R009):** `alembic upgrade head` lokalde → migration 045 uygulanır, downgrade test edilir; `\d ds_schema_record_warnings` ile index ismi + check constraint görülür.
- **G4 (R013):** Hover üzerinden disabled checkbox üzerinde `not-allowed` görünür mü.
- **G5 (R014):** DevTools console — `applyFilterAndRender` spy; 3s tick'lerde state değişmiyorsa 0 call.
- **G6 (R016):** Console — `await vyraFetch('/api/health', { responseType: 'blob' })` Response döner; default JSON path bozulmaz.
- **G7 (R017):** Console — `await vyraFetchUI('/api/health', {}, { onSuccess: console.log })` sırayla loading→success.

Final regression: `python -c "import py_compile; py_compile.compile('app/api/routes/query_builder_api.py', doraise=True)"` ve mevcut pytest (`tests/db_smart/...` ilgili olanlar).

## 8. Out-of-scope

- R016 call-site migration (yeni PR — R019).
- R017 28 modülde adoption (yeni PR — R020).
- KAP-12 broader IDOR sweep (agentic_query_api / schema_browse_api / db_smart_api).
- Credential rotation policy (R018 notes'ta yer alıyor).
- v3.34.0 → v3.34.1 sürüm bump (R018 hotfix sebebiyle bu sprint sonunda yapılacak — kullanıcı onayına bırakıldı).
